{
  "name": "pos-seller-web",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && pnpm dev",
    "dev:core": "cd services/core && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "dev": "pnpm run /^dev:/",
    "services:up": "docker compose up --build",
    "services:down": "docker compose down"
  }
}