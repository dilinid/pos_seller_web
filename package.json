{
  "name": "pos-seller-web",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "cd backend && uvicorn main:app --reload --host 0.0.0.0 --port 5000",
    "dev": "pnpm run /^dev:/"
  }
}